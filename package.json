{
  "name": "traders-ai-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Static Deriv trading bot workspace",
  "scripts": {
    "build": "node -e \"console.log('Static site: no build step required')\"",
    "dev": "python3 -m http.server ${PORT:-3000}",
    "start": "python3 -m http.server ${PORT:-3000}"
  }
}
